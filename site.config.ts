interface SiteConfig {
    site_name: string;
    site_description: string;
}

const siteConfig: SiteConfig = {
    site_name: 'WP Headless with Next.js',
    site_description: 'A headless WordPress blog powered by Next.js',
};

export { siteConfig };
